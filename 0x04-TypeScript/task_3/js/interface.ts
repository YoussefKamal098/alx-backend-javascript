// Create a type RowID and set it equal to number
export type RowID = number;

// Create an interface RowElement with fields
export interface RowElement {
    firstName: string;
    lastName: string;
    age?: number; // Optional field
}
